# Project: PiecePool (LLM-Wiki 기반 개인용 지식베이스 프로젝트)

## 1. Project Overview & Core Philosophy
본 프로젝트(PiecePool)는 안드레이 카파시(Andrej Karpathy)의 'LLM-Wiki' 철학을 기반으로, LLM 에이전트가 능동적으로 관리하는 개인용 지식베이스(Knowledge Base)를 구축하는 것을 목표로 합니다.

### 核心 아이디어 (Core Concept)
- **RAG 방식의 한계 극복**: 기존 RAG(NotebookLM, ChatGPT 등)는 사용자가 업로드한 원시 문서에서 쿼리가 발생할 때마다 정보를 처음부터 다시 검색하므로 정보가 축적되지 않는 한계가 있음.
- **점진적 지식 축적 (Living Wiki)**: 쿼리 발생 시마다 원시 문서를 그대로 가져오는 대신, 사용자와 원시 데이터 사이에 **마크다운(md) 형식의 구조화된 정보(The Wiki)**를 위치시켜 점진적으로 지식을 축적하고 관리함.
- **LLM 중심의 유지보수**: LLM이 관련 페이지를 업데이트하고, 주제별 요약을 수정하며, 새로운 데이터와 기존 정보 간의 모순을 기록하고 상호 참조(Cross-reference)를 유지함. 지식은 단 한 번만 수집(Ingest)되며, 매번 다시 생성되지 않고 지속적으로 발전함.

---

## 2. System Architecture: The Three Layers
PiecePool 시스템은 다음의 3개 레이어로 완전히 분리되어 작동합니다.

### Layer 1: Raw sources (진실의 원천)
- **정의**: 사용자가 선별하여 모아둔 변경 불가능한 원시 문서 집합 (기사, 논문, 이미지, 데이터 파일 등).
- **규칙**: 대규모 언어 모델(LLM)은 이 자료들을 오직 읽기(Read-only)만 할 수 있으며, 절대로 직접 수정하지 않음.

### Layer 2: The wiki (지식 저장소)
- **정의**: LLM이 생성하고 직접 관리하는 마크다운(*.md) 파일들의 목록 (요약문, 정보 페이지, 개념 페이지, 비교 표, 전체 개요 등).
- **규칙**: LLM이 페이지를 생성하고, 새 정보 유입 시 업데이트하며, 페이지 간 연관성을 유지함. 읽는 것은 인간이지만 작성·유지보수하는 것은 LLM임.

### Layer 3: The schema (시스템 설정 및 규칙)
- **정의**: 위키의 구성 방식, 적용 규칙, 정보 수집/답변/관리 절차를 LLM에게 지시하는 핵심 설정 문서.
- **PiecePool 확장 방향**: 비즈니스 모델(BM) 구축을 위해 이 스키마를 고도화하며, 위키의 구성 상태를 한눈에 보여주는 대시보드 기능을 포함함.

---

## 3. Operations & Lifecycle (운영/관리 프로세스)

### 3.1 데이터 수집 (Ingest)
1. 새로운 데이터를 원시 데이터(Raw sources) 모음에 추가하고 LLM에게 처리를 지시함.
2. LLM은 데이터를 읽고 중요 내용을 사용자와 논의함.
3. 위키에 요약문을 작성하고, 인덱스를 업데이트하며, 전반적으로 관련된 정보(10~15개 페이지 영향 가능)를 함께 수정함.
4. 마지막으로 `log.md`에 해당 데이터에 대한 기록을 추가함.
- **운영 원칙**: 대량 자동 처리도 가능하나, 사용자가 요약문과 업데이트를 확인하며 LLM의 중점 사항을 지시하는 '인간 개입형(Human-in-the-loop)' 방식을 권장하며, 선택한 방식을 스키마에 반드시 명시해야 함.

### 3.2 질문하기 (Query)
1. 사용자가 위키에 질문을 던지면, LLM은 관련 페이지를 검색·독해한 뒤 인용문과 함께 답변을 작성함.
2. 답변 형태는 요구에 따라 마크다운 페이지, 비교 표, 슬라이드 데크(Marp), 차트(matplotlib), 캔버스 등 다양하게 출력함.
3. **핵심 규칙**: 도출된 좋은 답변(비교 결과, 분석 내용, 발견된 연관성 등)은 채팅 기록 속으로 사라지지 않도록 **반드시 위키의 새로운 페이지로 저장**하여 지식베이스에 축적함.

### 3.3 주기적 정리 (Lint)
- 사용자는 주기적으로 LLM에게 위키의 건강 상태 점검을 요청함.
- **체크리스트**: 페이지 간 모순된 내용, 최신 자료 도입으로 무효화된 정보, 외부 링크가 없는 고립된 페이지, 언급되었으나 별도 페이지가 없는 중요 개념, 누락된 참조 정보, 웹 검색을 통해 보완 가능한 데이터 누락분.
- LLM은 조사해야 할 새로운 주제나 찾아봐야 할 신규 자료를 사용자에게 제안함.

---

## 4. Indexing & Logging Spec (인덱싱 및 로깅 사양)

위키의 효과적인 파악을 위해 LLM과 사용자는 다음 두 가지 특별한 파일을 유지 관리합니다.

### `index.md` (콘텐츠 중심 구조)
- 위키에 존재하는 모든 페이지의 마크다운 목록 파일.
- **포함 데이터**: 페이지 링크, 간단한 요약문, 메타데이터(날짜, 출처 수 등). 엔티티/개념/출처 등 범주별로 분류.
- **구동 방식**: LLM은 데이터 업데이트 시 인덱스도 동시 업데이트함. 쿼리 발생 시 LLM은 이 인덱스를 먼저 읽어 관련 페이지를 추적함.
- **장점**: 약 100개의 출처, 수백 페이지 규모에서는 임베딩 기반의 복잡한 RAG 인프라 없이도 완벽히 작동함.

### `log.md` (시간 순서 기록 구조)
- 위키 내 모든 이벤트의 타임라인 기록 파일 (데이터 입력, 쿼리 수행, 린트 완료 등).
- **로그 작성 컨벤션**: Unix CLI 도구로 파싱 가능하도록 특정 접두사를 고정하여 작성함.
  - *예시*: `## [2026-04-02] ingest | Article Title`
- **분석 활용**: 사용자는 `grep "^## \[" log.md | tail -5`와 같은 명령어로 최근 작업을 빠르게 모니터링할 수 있으며, LLM도 최근 작업 맥락을 이해하는 데 활용함.

---

## 5. Tooling & Ecosystem Integration (도구 및 에코시스템 연계)

### 5.1 CLI Tools (`qmd`)
- 위키 규모 확장 시 로컬 마크다운 검색 엔진 `qmd`를 도입하거나 간단한 스크립트를 빌드함.
- `qmd`는 BM25와 벡터 검색을 혼합하고 LLM 리랭킹을 지원하며, 온디바이스로 동작함. CLI 및 MCP(Model Context Protocol) 서버를 통해 LLM이 네이티브 도구처럼 활용 가능함.
- *참조*: https://github.com/tobi/qmd

### 5.2 Obsidian 활용 팁 및 연동
- **Obsidian Web Clipper**: 웹 기사를 마크다운으로 즉시 변환하여 데이터베이스에 저장.
- **로컬 이미지 다운로드 및 LLM 참조**: 첨부 파일 경로를 `raw/assets/`로 지정하고 단축키를 통해 이미지를 로컬에 다운로드함. LLM이 마크다운 내장 이미지를 한 번에 읽지 못하므로, **텍스트를 먼저 읽은 후 참조된 이미지 파일을 별도로 확인**하도록 프로세스를 구성함.
- **Marp**: 마크다운 기반 플러그인을 활용해 위키 콘텐츠 기반의 프레젠테이션/슬라이드 데크 즉시 생성.
- **Dataview**: 페이지 상단 YAML 프론트마터(태그, 날짜, 소스 수 등)를 기반으로 동적 표와 목록을 생성.
- **Git 버전 관리**: 전체 위키는 마크다운 파일 기반의 Git 리포지토리로 관리하므로 버전 관리, 브랜칭, 협업이 완전히 무료로 지원됨.

---

## 6. PiecePool 독자적 차별화 포인트 (Our Project Spec)
- **시각적 노드 디테일화**: 옵시디안의 기본 그래프 뷰(Graph View) 기능을 고도화하여, 사용자가 단순히 연결선만 보는 것이 아니라 **노드의 세부 내용도 간편하게 미리 볼 수 있는 기능**을 제공함.
- **인간 중심의 지식 연결 (Human-Driven Connection)**: 린트(Lint) 단계 등에서 어떤 정보가 고립되어 있는지 시스템이 찾아내면, 그 **정보 간의 의미론적 연결과 최종 관계 수립은 AI가 아닌 '인간이 해야 할 일'로 정의**하여 인간과 LLM의 협업 시너지를 극대화함.

---

## 7. Active Task (현재 진행 중인 작업)
- **목표**: 상기 작성된 LLM-Wiki 및 PiecePool 핵심 사양 명세서를 기반으로, 우리 프로젝트의 백엔드/프론트엔드 아키텍처 및 데이터 흐름(Data Flow)에 어떻게 이식할 것인지 구체적인 구현 전략을 수립할 것.
- **요구사항**: 
  1. 원시 소스(`raw/`) 보존 및 `index.md`, `log.md`를 LLM이 자동으로 읽고 쓸 수 있도록 하는 파일 시스템 인터페이스 설계.
  2. PiecePool의 차별점인 '고립된 노드 탐지' 및 '인간의 정보 연결 인터페이스' 구현 방안 구상.