# PiecePool 여정 기록 (Idea → Product)

아이디어가 제품이 되기까지의 **큰 사건만** 남기는 경량 로그. 보고서·사업 자료·PPT·아이디어 발전 과정 회고의 원재료로 쓴다.

> 규칙: 큰 변화(주요 기능·확정 결정·피벗)마다 표에 **한 줄** 추가. 기술 상세는 쓰지 않고 ADR/스펙 링크로 대신한다. "의미" 칸은 PPT 슬라이드에 그대로 옮길 수 있는 문장으로 쓴다.
> **누가 남기나**: 해당 PR **작성자(기여자) 본인**이 merge 전에 같은 PR 안에서 추가한다 — 누가 merge하든 기록이 함께 들어온다. 강제 장치 3중: PR 템플릿 체크리스트 · [CONTRIBUTING](../../CONTRIBUTING.md) 규칙 · **Claude Code 공유 훅**(`scripts/hooks/journey-guard.sh` — feat 커밋 PR인데 본 문서 변경이 없으면 `gh pr create` 차단). 사소한 변경(deps·오타·리팩터링)은 남기지 않는다.

---

## 1. 원점 — 문제와 아이디어

- **문제**: 강의 중 이해 속도가 진도를 못 따라간다 → 필기는 논리정연한 글이 아니라 **파편**(불릿·반문장·화살표)으로만 쌓인다. 파편은 복습 가치가 낮고, 버려진다.
- **아이디어**: 파편을 버리지 않는다. AI가 파편을 **개념 위키 + 지식 그래프 + 논리정연한 정리 글**로 재구성한다.
- **차별점 3가지**: ① 원본 불변 — 사용자의 필기는 절대 덮어쓰지 않는다(archive 계약) ② 근거 추적 — 모든 개념·관계가 출처(provenance)를 갖는다 ③ API 키 없이도 전 기능 동작(오프라인 폴백).

---

## 2. 마일스톤 타임라인

| 날짜 | 사건 | 의미 (보고서용 한 줄) |
|---|---|---|
| ~06-27 | PRD → 계약 우선(SSOT) 설계 체계 수립 | 코드보다 계약을 먼저 — `docs/10-contracts/`가 팀 4역할의 단일 진실 |
| 06-28~30 | 설계 문서 라운드 (파이프라인·에러·화면·agentic 계층) | 문서 리뷰로 아키텍처를 먼저 검증하고 구현 착수 |
| 06-29 | 로컬 LLM provider 실험 (#71, #73) | 무료 로컬 추론 가설 검증 시도 |
| 06-30 | **피벗: OpenAI 단일 provider** (#78, [ADR-0001](../adr/0001-llm-provider-openai.md)) | 로컬 LLM·freemium 폐기 — 경진대회 품질·일정에 집중하는 사업 판단 |
| 07-01 | **데스크톱 앱 전기능 슬라이스** (#79) | Import→위키→그래프→검색이 실제로 도는 첫 제품 |
| 07-01 | 저장소 기업식 정리 P1~P6 (#80~#96) | 라이선스(독점)·CI·릴리스 체계 — 제출 가능한 저장소 |
| 07-01 | **Liner API 채택** (#102·#103) | 정보 간극 메우기(feature 3)의 주 엔진 = 권위 출처 검색 |
| 07-01 | **시멘틱 청킹 파이프라인** (#104) | 정보를 "의미 경계"로 자르고 연결성 게이트로 승격 — 지식 그래프의 품질 장치 |
| 07-02 | **Study Vault 리디자인** (#110) | Obsidian 셸 + Notion 슬래시 에디터 — 학습 도구다운 첫인상 |
| 07-02 | 판매 수준 폴리시 (#111) | 리사이즈·DnD·분할 뷰·파일 관리 — "데모"에서 "제품"으로 |
| 07-02 | 그래프 2단 진화 (#113→#114) | 물리 레이아웃 → **논리 기반 계층 + 모노크롬 엣지 언어** — 그래프가 장식이 아니라 의미를 나름 |
| 07-02 | PDF 업로드 → AI 요약·정리 (#115) | 교수 자료(label)를 파이프라인의 1급 입력으로 |
| 07-02 | Study Home·문서 뷰 폴리시 + 미니 관계 그래프 (#116) | 위키 페이지 안에서 개념의 이웃 관계를 한눈에 |
| 07-02 | **파편 → 정리 글 스트리밍 변환** (#117, [ADR-0008](../adr/0008-note-synthesis-streaming.md)) | 원점 아이디어의 직접 구현 — 파편이 눈앞에서 논리정연한 글이 된다 |
| 07-03 | PDF 뷰어 작업 공간화 — 줌·연속 스크롤·썸네일 레일 | 원본 자료를 "미리보기"에서 "필기 옆에 두고 읽는 작업 공간"으로 |
| 07-03 | **Obsidian UI 정밀 리스킨** — 다크 기본 + overlay 타이틀바 + 탭 최상단 | 검증된 도구(Obsidian)의 시각 언어를 그대로 — 학생이 배우지 않고도 쓸 수 있는 화면 |
| 07-03 | 트리 드래그-아웃 — 노트를 끌어 외부 앱/LLM 프롬프트에 파일로 드롭 | 지식이 앱 안에 갇히지 않는다 — 내 필기가 어디서든 재료가 되는 열린 볼트 |
| 07-03 | 탭 "+" 버튼 → 새 노트 즉시 생성 | 쓰기 시작까지의 마찰 제거 — 빈 화면 대신 새 노트가 바로 열린다 |
| 07-03 | Study Home 포커스 재설계 + 친근한 네이밍 | 첫 화면은 핵심 행동 2개(새 노트·개념 지도)만 크게, 상세는 접었다 펼치면 자연스레 정렬 — 기술어(그래프·지식 공간)를 학생 말(지도·나의 학습 공간)로 바꿔 첫인상의 문턱을 낮췄다 |
| 07-03 | 새 폴더(지식 공간) 생성 — 사이드바 UI + Rust `create_space` | 사용자가 과목/주제별 학습 공간을 직접 만든다. 문서로만 정의하던 백엔드 계약이 실제 Rust 커맨드로 구현된 첫 사례 — 버튼 클릭이 IPC를 거쳐 파일시스템에 폴더를 만드는 풀스택 한 바퀴 완성 |
| 07-06 | **노드 우선도 알고리즘** — 구조 팩터(중심성·엣지품질) 기반 파생 점수로 그래프 노드 크기 구동 ([prioritization.md](../20-backend/prioritization.md)) | 그래프가 "무엇이 연결됐나"에서 "무엇이 중요한가"로 진화 — 허브 개념이 크기로 한눈에 드러난다. 이후 클릭 빈도(주목도)까지 반영할 토대 |
| 07-06 | Inbox 저장 위치(폴더) 선택 — 노트·위키를 원하는 지식 공간에 바로 저장 | 수집의 마지막 마찰 제거 — "어디에 담지?"를 저장하는 순간 고른다. 폴더를 먼저 만들고 그 안에 바로 필기하는 자연스러운 학습 흐름 완성 |
| 07-06 | **Notion풍 노트 페이지** — 아이콘·큰 제목(인라인 이름 변경)·속성(과목 지정·사이드바 고정·생성일)·관계형(연결된 노트 추가) 헤더 + Rust `update_note_subjects` | 노트가 "파일"에서 "페이지"로 — Notion에 익숙한 학생이 배우지 않고 과목을 달고, 고정하고, 개념을 손으로 잇는다. 관계 그래프에 사용자가 직접 엣지를 추가하는 첫 UI |
| 07-06 | **새 탭 = 런처** — "+"가 빈 파일을 만들지 않고 검색·최근 문서·고정·시작 액션 화면을 연다 (VS Code/Arc의 "탐색 트리거에 생성 부작용 금지" 관례) | "제목 없음" 빈 파일이 쌓이던 문제를 구조로 차단 — 새 탭은 재진입(어제 보던 필기로 점프), 생성은 Inbox(저장 시에만 파일 생성)로 역할 분리 |
| 07-06 | **슬래시 메뉴 Notion급 리디자인** — 섹션 헤더·테두리 없는 아이콘 타일·키캡 단축키·매칭 글자 액센트 | `/`로 블록을 부르는 순간이 학습 도구의 첫 손맛 — 검증된 Notion 슬래시 메뉴의 조용한 미감을 그대로 가져오고, 타이핑하면 매칭 글자만 강조돼 원하는 블록을 눈으로 좇는다 |
| 07-07 | 관계 품질 점검 — 위키 화면 "관계 품질" 버튼으로 related_to 비율(기준 30%)·저신뢰(confidence) 관계 개수를 보고 기준을 직접 조절 | AI가 만든 그래프를 사용자가 감사한다 — "연결이 너무 느슨한가?"를 수치로 드러내 지식 그래프의 품질 책임을 사용자에게 돌려준다 |
| 07-07 | 사이드바 네비게이션 폴리시 — 볼트 전환란 테마 반응 드롭업(라이트=흰·다크=검), 볼트 바꾸면 랜덤 문서 대신 Study Home 착지, 고정 문서 우클릭 → 고정 해제 | 학습 공간 사이를 오가는 순간의 마찰과 이질감 제거 — 테마와 어긋나던 흰 팝업을 없애고, "공간을 바꿨는데 웬 문서?"를 맥락 착지로 바로잡아 볼트가 조용한 홈처럼 느껴지게 했다 |
| 07-07 | 사이드바 파일 정렬 — 이름/업데이트/생성일 × 오름·내림 드롭다운(persist) | 파일이 많아져도 원하는 순서로 훑는다 — 최근 고친 것부터, 이름 가나다순으로. 정렬 기준을 저장해 다음 실행에도 유지된다 |

---

## 3. 아이디어 발전 과정 (PPT 스토리라인용 5막)

1. **문제 발견** — "필기가 파편으로만 남는다"는 학습자의 보편 문제에서 출발.
2. **구조 설계** — 코드 전에 계약(엔티티·관계 12종·파일 레이아웃)을 확정. 팀 4역할이 같은 언어로 말하게 됨.
3. **현실 피벗** — 로컬 LLM 실험 후 OpenAI 단일로 축소. "다 지원"보다 "하나를 제대로"가 대회 전략.
4. **지식의 품질 장치** — 시멘틱 청킹(의미 경계 분할) + 연결성 게이트(고립 노드 staging) + 출처 신뢰도(Liner) — 그래프가 쓰레기장이 되지 않게 하는 3중 장치.
5. **원점 회귀** — 마지막에 다시 첫 문제로: 파편을 **실시간 스트리밍**으로 정리 글로 바꿔 보여준다. 수집(Inbox) → 구조화(위키·그래프) → 재구성(정리 글)의 학습 루프 완성.

---

## 4. 확정된 핵심 결정 (전체는 [ADR 인덱스](../adr/README.md))

| 결정 | 왜 (사업적 이유) |
|---|---|
| OpenAI 단일 + Liner 출처 검색 | 품질 예측 가능 + 근거 추적이라는 차별점 강화 |
| 원본(archive) LLM 불가침 | 신뢰 — 사용자의 기록을 AI가 절대 훼손하지 않는다 |
| 키 없이 전 기능(휴리스틱 폴백) | 데모·심사 환경 어디서든 돌아간다 |
| 계약 변경 = 4역할 승인 | 속도보다 정합성 — 소규모 팀의 스케일 장치 |
| 정리 글 = 기존 WikiPage 재사용 | 새 개념(엔티티) 추가 없이 기능 확장 — 단순함 유지 |

---

## 5. 변경 이력 노트

- 2026-07-03 @gosu1 — 최초 작성 (#117 머지 시점까지 정리).
