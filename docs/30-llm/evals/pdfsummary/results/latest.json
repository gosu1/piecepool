{
  "id": "pdfsummary",
  "dry": false,
  "model": "gemini-3.1-flash-lite",
  "baseUrl": "https://generativelanguage.googleapis.com/v1beta/openai",
  "judgeModel": "gemini-3.5-flash",
  "metrics": {
    "cases": 4,
    "runFailed": 0,
    "sectionRecall": 1,
    "termRecall": 0.8823529411764706,
    "absentFactLeak": 0,
    "formulaBroken": 2,
    "notKorean": 0,
    "unexpectedTruncation": 0,
    "charsPerSectionMin": 295.3333333333333,
    "calloutCompliance": 1,
    "ttftMsMax": 10519,
    "totalMsMax": 17364,
    "hallucination": 2,
    "judgeFail": 2
  },
  "gateFails": [
    "수식 기호 유실 0건 — 실측 2 (허용 <= 0)",
    "환각 0건 (잠정) — 실측 2 (허용 <= 0)",
    "judge 실패 0건 — 실측 2 (허용 <= 0)"
  ],
  "samples": [
    {
      "fixture": {
        "id": "formula-heavy",
        "input": {
          "sourceTitle": "Lecture 7. Optimization for Neural Networks",
          "sourceText": "Lecture 7. Optimization for Neural Networks\n\n7.1 Gradient Descent\nLet $\\theta \\in \\mathbb{R}^d$ be the parameter vector, $\\alpha > 0$ the learning rate, and $L(\\theta)$ the training loss.\nThe update rule is\n$$\\theta_{t+1} = \\theta_t - \\alpha \\nabla_\\theta L(\\theta_t)$$\nIn SGD the gradient is estimated on a mini-batch $B$ with $|B| = m$ examples:\n$$\\nabla_\\theta L \\approx \\frac{1}{m} \\sum_{i \\in B} \\nabla_\\theta \\ell(x_i, y_i; \\theta)$$\n\n7.2 Momentum\nMomentum keeps an exponentially decayed average of past gradients with coefficient $\\beta \\in [0, 1)$:\n$$v_{t+1} = \\beta v_t + (1 - \\beta) \\nabla_\\theta L(\\theta_t), \\qquad \\theta_{t+1} = \\theta_t - \\alpha v_{t+1}$$\nTypical values are $\\beta = 0.9$ and $\\alpha = 10^{-3}$.\n\n7.3 Softmax\nFor a logit vector $z \\in \\mathbb{R}^K$ the softmax is\n$$\\sigma(z)_i = \\frac{e^{z_i}}{\\sum_{j=1}^{K} e^{z_j}}$$\nso that $\\sum_{i=1}^{K} \\sigma(z)_i = 1$ and $\\sigma(z)_i > 0$ for every $i$.\n\n7.4 Cross-Entropy\nFor a one-hot target $y$ and prediction $\\hat{y} = \\sigma(z)$ the cross-entropy loss is\n$$L = -\\sum_{i=1}^{K} y_i \\log \\hat{y}_i$$\nIts gradient with respect to the logits collapses to $\\partial L / \\partial z = \\hat{y} - y$, which is why softmax and cross-entropy are fused into a single op.\n\n7.5 Learning Rate Schedule\nStep decay multiplies $\\alpha$ by $\\gamma = 0.1$ every $s$ epochs: $\\alpha_t = \\alpha_0 \\gamma^{\\lfloor t / s \\rfloor}$.\nCosine annealing instead follows $\\alpha_t = \\alpha_{\\min} + \\frac{1}{2}(\\alpha_0 - \\alpha_{\\min})(1 + \\cos(\\pi t / T))$."
        },
        "expectSections": [
          "Gradient Descent",
          "Momentum",
          "Softmax",
          "Cross-Entropy"
        ],
        "expectTerms": [
          "SGD",
          "logit",
          "mini-batch"
        ],
        "absentFacts": [
          "드롭아웃",
          "배치 정규화",
          "과적합"
        ],
        "expectFormula": "\\alpha",
        "whyHard": "원문이 이미 KaTeX 로 쓰여 있고 그리스 문자(alpha·beta·theta·sigma·gamma)와 첨자가 절마다 나온다. 서술을 한국어로 옮기면서 기호를 풀어 쓰면(예: alpha 를 '학습률'로만) 원문 대조가 깨진다."
      },
      "out": {
        "markdown": "# 요약\n본 문서는 신경망 학습을 위한 최적화 기법과 손실 함수, 학습률 조정 방법을 다룬다. 경사 하강법과 모멘텀을 이용한 매개변수 갱신, 소프트맥스와 교차 엔트로피를 통한 분류 모델의 출력 및 손실 계산, 그리고 학습률을 시간에 따라 변화시키는 스케줄링 기법을 설명한다.\n\n## 1. 경사 하강법(Gradient Descent)\n매개변수 벡터 $\\theta$를 학습률 $\\alpha$와 손실 함수 $L(\\theta)$의 기울기를 사용하여 갱신한다.\n- 갱신 규칙: $\\theta_{t+1} = \\theta_t - \\alpha \\nabla_\\theta L(\\theta_t)$\n- 확률적 경사 하강법(Stochastic Gradient Descent, SGD): 전체 데이터 대신 크기가 $m$인 미니 배치 $B$를 사용하여 기울기를 근사한다.\n- 근사식: $\\nabla_\\theta L \\approx \\frac{1}{m} \\sum_{i \\in B} \\nabla_\\theta \\ell(x_i, y_i; \\theta)$\n\n> [!easy] 쉬운 설명\n> **한마디로:** 산 정상에서 가장 가파른 내리막길을 찾아 조금씩 내려가는 과정이다.\n>\n> #### 핵심 원리\n> - **기울기:** 손실이 가장 크게 줄어드는 방향을 가리킨다.\n> - **미니 배치:** 전체 데이터를 다 보지 않고 일부만 뽑아 빠르게 방향을 결정한다.\n\n## 2. 모멘텀(Momentum)\n과거의 기울기 정보를 지수적으로 감소하는 평균으로 유지하여 갱신 방향에 관성을 부여한다.\n- 갱신 규칙: $v_{t+1} = \\beta v_t + (1 - \\beta) \\nabla_\\theta L(\\theta_t)$\n- 매개변수 적용: $\\theta_{t+1} = \\theta_t - \\alpha v_{t+1}$\n- 계수 $\\beta$는 보통 0.9, 학습률 $\\alpha$는 $10^{-3}$을 주로 사용한다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 언덕을 내려갈 때 가속도가 붙어 더 빠르게 내려가는 공과 같다.\n>\n> #### 주요 요소\n> - **관성:** 이전의 움직임을 기억해 방향이 급격히 바뀌지 않게 돕는다.\n> - **계수:** 과거의 정보를 얼마나 반영할지 결정하는 수치이다.\n\n## 3. 소프트맥스(Softmax)\n로짓(logit) 벡터 $z$를 입력받아 각 요소가 0보다 크고 합이 1이 되도록 변환한다.\n- 함수식: $\\sigma(z)_i = \\frac{e^{z_i}}{\\sum_{j=1}^{K} e^{z_j}}$\n- 결과값은 각 클래스에 속할 확률로 해석할 수 있다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 여러 개의 점수를 확률로 바꾸어 합계를 100퍼센트로 만드는 작업이다.\n>\n> #### 특징\n> - **확률화:** 모든 출력값의 합은 항상 1이다.\n> - **양수화:** 지수 함수를 사용하여 모든 값을 0보다 크게 만든다.\n\n## 4. 교차 엔트로피(Cross-Entropy)\n원-핫(one-hot) 인코딩된 정답 $y$와 예측값 $\\hat{y}$ 사이의 차이를 측정하는 손실 함수이다.\n- 손실식: $L = -\\sum_{i=1}^{K} y_i \\log \\hat{y}_i$\n- 로짓에 대한 기울기: $\\partial L / \\partial z = \\hat{y} - y$\n- 기울기가 단순하게 계산되므로 소프트맥스와 교차 엔트로피를 하나의 연산으로 통합하여 처리한다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 정답과 예측이 얼마나 다른지 계산하여 오차를 측정하는 방법이다.\n>\n> #### 계산 효율\n> - **통합 연산:** 기울기 계산이 간결해져 학습 속도가 빨라진다.\n\n## 5. 학습률 스케줄(Learning Rate Schedule)\n학습이 진행됨에 따라 학습률 $\\alpha$를 조절하여 최적화 효율을 높인다.\n- 단계적 감소(Step decay): 일정 에폭(epoch)마다 $\\alpha$에 $\\gamma=0.1$을 곱하여 줄인다.\n- 코사인 어닐링(Cosine annealing): 코사인 함수를 사용하여 학습률을 부드럽게 감소시킨다.\n- 코사인 식: $\\alpha_t = \\alpha_{\\min} + \\frac{1}{2}(\\alpha_0 - \\alpha_{\\min})(1 + \\cos(\\pi t / T))$\n\n> [!easy] 쉬운 설명\n> **한마디로:** 목적지에 가까워질수록 보폭을 줄여 정확하게 멈추게 하는 전략이다.\n>\n> #### 감소 방식\n> - **단계적:** 특정 시점마다 보폭을 뚝 떨어뜨린다.\n> - **코사인:** 곡선을 그리며 서서히 보폭을 줄여나간다.",
        "sectionHits": 4,
        "termHits": 2,
        "absentHits": [],
        "formulaKept": true,
        "korean": true,
        "truncated": false,
        "truncationMismatch": false,
        "charsPerSection": 409.25,
        "calloutSections": 5,
        "calloutCompliant": 5,
        "ttftMs": 863,
        "totalMs": 5662
      },
      "latencyMs": 5662
    },
    {
      "fixture": {
        "id": "lecture-slide",
        "input": {
          "sourceTitle": "Chapter 3. Process Scheduling",
          "sourceText": "Chapter 3. Process Scheduling\n\n3.1 Objectives\nMaximize CPU utilization. Minimize turnaround time.\n\n3.2 Algorithms\nFCFS: first come first served, non-preemptive.\nSJF: shortest job first, optimal average waiting time.\nRound Robin: time quantum q, preemptive.\n\n3.3 Formula\nWaiting time W = T_turnaround - T_burst."
        },
        "expectSections": [
          "Objectives",
          "Algorithms",
          "Formula"
        ],
        "expectTerms": [
          "FCFS",
          "SJF",
          "Round Robin"
        ],
        "absentFacts": [
          "교착상태",
          "페이지 폴트",
          "세마포어"
        ],
        "expectFormula": "T_turnaround",
        "whyHard": "영어 원문을 한국어로 요약하면서 알고리즘 이름과 수식 기호는 보존해야 한다."
      },
      "out": {
        "markdown": "# 요약\n프로세스 스케줄링은 중앙처리장치(CPU)의 효율을 극대화하고 작업 완료 시간을 최소화하는 것을 목표로 한다. 이를 위해 선입선출, 최단 작업 우선, 라운드 로빈 등 다양한 알고리즘이 사용된다. 대기 시간은 전체 반환 시간에서 실제 작업 수행 시간을 빼서 계산한다.\n\n## 1. 목적(Objectives)\n- CPU 이용률을 최대화하는 것을 목표로 한다.\n- 작업이 제출된 후 완료되기까지 걸리는 반환 시간(turnaround time)을 최소화한다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** CPU가 쉬지 않고 일을 처리하여, 사용자가 결과를 빨리 받게 만드는 것이 목표입니다.\n>\n> #### 핵심 목표\n> - **CPU 이용률:** CPU가 노는 시간 없이 최대한 바쁘게 움직이게 함.\n> - **반환 시간:** 작업 시작부터 끝까지 걸리는 시간을 최대한 줄임.\n\n## 2. 알고리즘(Algorithms)\n- 선입선출(FCFS, First Come First Served): 먼저 도착한 프로세스를 먼저 처리하며, 중간에 작업을 멈출 수 없는 비선점형 방식이다.\n- 최단 작업 우선(SJF, Shortest Job First): 작업 시간이 짧은 프로세스를 먼저 처리하며, 평균 대기 시간을 최소화하는 최적의 방식이다.\n- 라운드 로빈(Round Robin): 정해진 시간 단위인 시간 할당량(time quantum) $q$만큼씩 돌아가며 처리하는 선점형 방식이다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 줄을 서서 기다리는 사람들을 어떤 순서로 처리할지 정하는 규칙들입니다.\n>\n> #### 주요 방식\n> - **선입선출:** 먼저 온 손님부터 차례대로 처리.\n> - **최단 작업 우선:** 빨리 끝날 일을 먼저 해서 기다리는 사람을 줄임.\n> - **라운드 로빈:** 모두에게 공평하게 정해진 시간만큼만 일하고 다음 사람으로 넘어감.\n\n## 3. 공식(Formula)\n- 대기 시간(Waiting time) $W$는 전체 반환 시간 $T_{turnaround}$에서 실제 작업 수행 시간인 버스트 시간(burst time) $T_{burst}$를 뺀 값이다.\n\n$$W = T_{turnaround} - T_{burst}$$\n\n> [!easy] 쉬운 설명\n> **한마디로:** 전체 걸린 시간에서 실제로 일한 시간을 빼면, 가만히 기다린 시간만 남습니다.\n>\n> #### 계산 원리\n> - **대기 시간:** 전체 소요 시간에서 순수하게 CPU가 일한 시간을 제외한 나머지.",
        "sectionHits": 3,
        "termHits": 3,
        "absentHits": [],
        "formulaKept": false,
        "korean": true,
        "truncated": false,
        "truncationMismatch": false,
        "charsPerSection": 295.3333333333333,
        "calloutSections": 3,
        "calloutCompliant": 3,
        "ttftMs": 3227,
        "totalMs": 5465
      },
      "latencyMs": 5465
    },
    {
      "fixture": {
        "id": "long-truncated",
        "input": {
          "sourceTitle": "Chapter 12. Consensus and Replication in Distributed Systems",
          "sourceText": "Chapter 12. Consensus and Replication in Distributed Systems\n\n12.1 Consensus\nConsensus lets a set of $N$ replicas agree on a single value even when some of them crash. A correct protocol guarantees agreement (no two replicas decide differently), validity (the decided value was proposed) and termination. Paxos was the first practical protocol; Raft restates the same guarantees in a form built around a single leader, which is why most production systems implement Raft rather than Paxos.\n\n12.2 Raft\nRaft splits time into terms. Each term begins with an election in which a candidate collects votes over RPC from the other replicas. A follower that hears nothing for the election timeout $t_{elect}$ becomes a candidate; the timeout is randomized in $[t_{elect}, 2 t_{elect}]$ so that two candidates rarely start at the same instant. The leader appends entries to its log and replicates them with AppendEntries RPC; an entry is committed once it is stored on a majority.\n\n12.3 Quorum\nA quorum system with $N$ replicas, write quorum $W$ and read quorum $R$ returns fresh data whenever\n$$W + R > N$$\nbecause any read set then intersects any write set. Setting $W = N$ makes writes slow and reads cheap; $W = R = \\lceil (N+1)/2 \\rceil$ balances the two. The CAP theorem says that under a network partition the system must give up either consistency or availability, and the quorum sizes are how that trade-off is dialled in.\n\n12.4 Replication\nSynchronous replication acknowledges a write only after the followers have persisted it, so a leader crash loses nothing but every write pays the slowest follower's latency. Asynchronous replication acknowledges immediately and can lose the tail of the log. Chain replication puts the replicas in a line: writes enter at the head, reads are served by the tail, and the tail's reply proves that every replica in the chain already holds the write.\n12.5 Log Compaction\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.6 Membership Change\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.7 Read Paths\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.8 Clock Assumptions\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.9 Failure Detection\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.10 Batching and Pipelining\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.11 Storage Durability\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.12 Client Sessions\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.13 Observability\nUseful signals are commit latency, the gap between the leader's last index and each follower's match index, term changes per hour, and snapshot size. A rising term counter with no crashes usually means the election timeout is too aggressive for the link. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.14 Testing\nDeterministic simulation runs the whole cluster in one process with a controlled scheduler and an injected network that drops, delays and reorders messages. Replaying a seed reproduces a failure exactly, which is the only practical way to debug a rare split-brain. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.15 Log Compaction (continued, part 2)\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.16 Membership Change (continued, part 2)\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.17 Read Paths (continued, part 2)\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.18 Clock Assumptions (continued, part 2)\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.19 Failure Detection (continued, part 2)\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.20 Batching and Pipelining (continued, part 2)\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.21 Storage Durability (continued, part 2)\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.22 Client Sessions (continued, part 2)\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.23 Observability (continued, part 2)\nUseful signals are commit latency, the gap between the leader's last index and each follower's match index, term changes per hour, and snapshot size. A rising term counter with no crashes usually means the election timeout is too aggressive for the link. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.24 Testing (continued, part 2)\nDeterministic simulation runs the whole cluster in one process with a controlled scheduler and an injected network that drops, delays and reorders messages. Replaying a seed reproduces a failure exactly, which is the only practical way to debug a rare split-brain. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.25 Log Compaction (continued, part 3)\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.26 Membership Change (continued, part 3)\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.27 Read Paths (continued, part 3)\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.28 Clock Assumptions (continued, part 3)\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.29 Failure Detection (continued, part 3)\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.30 Batching and Pipelining (continued, part 3)\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.31 Storage Durability (continued, part 3)\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.32 Client Sessions (continued, part 3)\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.33 Observability (continued, part 3)\nUseful signals are commit latency, the gap between the leader's last index and each follower's match index, term changes per hour, and snapshot size. A rising term counter with no crashes usually means the election timeout is too aggressive for the link. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.34 Testing (continued, part 3)\nDeterministic simulation runs the whole cluster in one process with a controlled scheduler and an injected network that drops, delays and reorders messages. Replaying a seed reproduces a failure exactly, which is the only practical way to debug a rare split-brain. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.35 Log Compaction (continued, part 4)\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.36 Membership Change (continued, part 4)\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.37 Read Paths (continued, part 4)\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.38 Clock Assumptions (continued, part 4)\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.39 Failure Detection (continued, part 4)\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.40 Batching and Pipelining (continued, part 4)\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.41 Storage Durability (continued, part 4)\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.42 Client Sessions (continued, part 4)\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.43 Observability (continued, part 4)\nUseful signals are commit latency, the gap between the leader's last index and each follower's match index, term changes per hour, and snapshot size. A rising term counter with no crashes usually means the election timeout is too aggressive for the link. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.44 Testing (continued, part 4)\nDeterministic simulation runs the whole cluster in one process with a controlled scheduler and an injected network that drops, delays and reorders messages. Replaying a seed reproduces a failure exactly, which is the only practical way to debug a rare split-brain. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.45 Log Compaction (continued, part 5)\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.46 Membership Change (continued, part 5)\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.47 Read Paths (continued, part 5)\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.48 Clock Assumptions (continued, part 5)\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.49 Failure Detection (continued, part 5)\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.50 Batching and Pipelining (continued, part 5)\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.51 Storage Durability (continued, part 5)\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.52 Client Sessions (continued, part 5)\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.53 Observability (continued, part 5)\nUseful signals are commit latency, the gap between the leader's last index and each follower's match index, term changes per hour, and snapshot size. A rising term counter with no crashes usually means the election timeout is too aggressive for the link. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.54 Testing (continued, part 5)\nDeterministic simulation runs the whole cluster in one process with a controlled scheduler and an injected network that drops, delays and reorders messages. Replaying a seed reproduces a failure exactly, which is the only practical way to debug a rare split-brain. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.55 Log Compaction (continued, part 6)\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.56 Membership Change (continued, part 6)\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.57 Read Paths (continued, part 6)\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.58 Clock Assumptions (continued, part 6)\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.59 Failure Detection (continued, part 6)\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.60 Batching and Pipelining (continued, part 6)\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.61 Storage Durability (continued, part 6)\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.62 Client Sessions (continued, part 6)\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.63 Observability (continued, part 6)\nUseful signals are commit latency, the gap between the leader's last index and each follower's match index, term changes per hour, and snapshot size. A rising term counter with no crashes usually means the election timeout is too aggressive for the link. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.64 Testing (continued, part 6)\nDeterministic simulation runs the whole cluster in one process with a controlled scheduler and an injected network that drops, delays and reorders messages. Replaying a seed reproduces a failure exactly, which is the only practical way to debug a rare split-brain. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.65 Log Compaction (continued, part 7)\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.66 Membership Change (continued, part 7)\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.67 Read Paths (continued, part 7)\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.68 Clock Assumptions (continued, part 7)\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.69 Failure Detection (continued, part 7)\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.70 Batching and Pipelining (continued, part 7)\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.71 Storage Durability (continued, part 7)\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.72 Client Sessions (continued, part 7)\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.73 Observability (continued, part 7)\nUseful signals are commit latency, the gap between the leader's last index and each follower's match index, term changes per hour, and snapshot size. A rising term counter with no crashes usually means the election timeout is too aggressive for the link. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.74 Testing (continued, part 7)\nDeterministic simulation runs the whole cluster in one process with a controlled scheduler and an injected network that drops, delays and reorders messages. Replaying a seed reproduces a failure exactly, which is the only practical way to debug a rare split-brain. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.75 Log Compaction (continued, part 8)\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.76 Membership Change (continued, part 8)\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.77 Read Paths (continued, part 8)\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.78 Clock Assumptions (continued, part 8)\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.79 Failure Detection (continued, part 8)\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.80 Batching and Pipelining (continued, part 8)\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.81 Storage Durability (continued, part 8)\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.82 Client Sessions (continued, part 8)\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.83 Observability (continued, part 8)\nUseful signals are commit latency, the gap between the leader's last index and each follower's match index, term changes per hour, and snapshot size. A rising term counter with no crashes usually means the election timeout is too aggressive for the link. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.84 Testing (continued, part 8)\nDeterministic simulation runs the whole cluster in one process with a controlled scheduler and an injected network that drops, delays and reorders messages. Replaying a seed reproduces a failure exactly, which is the only practical way to debug a rare split-brain. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.85 Log Compaction (continued, part 9)\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.86 Membership Change (continued, part 9)\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.87 Read Paths (continued, part 9)\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.88 Clock Assumptions (continued, part 9)\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.89 Failure Detection (continued, part 9)\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.90 Batching and Pipelining (continued, part 9)\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.91 Storage Durability (continued, part 9)\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.92 Client Sessions (continued, part 9)\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.93 Observability (continued, part 9)\nUseful signals are commit latency, the gap between the leader's last index and each follower's match index, term changes per hour, and snapshot size. A rising term counter with no crashes usually means the election timeout is too aggressive for the link. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.94 Testing (continued, part 9)\nDeterministic simulation runs the whole cluster in one process with a controlled scheduler and an injected network that drops, delays and reorders messages. Replaying a seed reproduces a failure exactly, which is the only practical way to debug a rare split-brain. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.95 Log Compaction (continued, part 10)\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.96 Membership Change (continued, part 10)\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.97 Read Paths (continued, part 10)\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.98 Clock Assumptions (continued, part 10)\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.99 Failure Detection (continued, part 10)\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.100 Batching and Pipelining (continued, part 10)\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.101 Storage Durability (continued, part 10)\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.102 Client Sessions (continued, part 10)\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.103 Observability (continued, part 10)\nUseful signals are commit latency, the gap between the leader's last index and each follower's match index, term changes per hour, and snapshot size. A rising term counter with no crashes usually means the election timeout is too aggressive for the link. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.104 Testing (continued, part 10)\nDeterministic simulation runs the whole cluster in one process with a controlled scheduler and an injected network that drops, delays and reorders messages. Replaying a seed reproduces a failure exactly, which is the only practical way to debug a rare split-brain. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.105 Log Compaction (continued, part 11)\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.106 Membership Change (continued, part 11)\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.107 Read Paths (continued, part 11)\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.108 Clock Assumptions (continued, part 11)\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.109 Failure Detection (continued, part 11)\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.110 Batching and Pipelining (continued, part 11)\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.111 Storage Durability (continued, part 11)\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.112 Client Sessions (continued, part 11)\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.113 Observability (continued, part 11)\nUseful signals are commit latency, the gap between the leader's last index and each follower's match index, term changes per hour, and snapshot size. A rising term counter with no crashes usually means the election timeout is too aggressive for the link. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.114 Testing (continued, part 11)\nDeterministic simulation runs the whole cluster in one process with a controlled scheduler and an injected network that drops, delays and reorders messages. Replaying a seed reproduces a failure exactly, which is the only practical way to debug a rare split-brain. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.115 Log Compaction (continued, part 12)\nA replicated log grows without bound, so each replica periodically writes a snapshot of its state machine and discards the prefix the snapshot covers. Snapshots are taken independently by each replica; the leader only ships a snapshot to a follower that has fallen so far behind that the required prefix is already gone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.116 Membership Change (continued, part 12)\nAdding or removing a replica cannot be done by editing a config file on every node at once, because two disjoint majorities could exist during the switch. The joint-consensus approach commits an intermediate configuration that requires majorities in both the old and the new set before the new set takes over alone. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.117 Read Paths (continued, part 12)\nA leader that has been partitioned away may still believe it is the leader and serve stale reads. A lease bounds how long that can last; alternatively the leader confirms its term with a round of heartbeats before answering, which costs one round trip per read. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.118 Clock Assumptions (continued, part 12)\nProtocols in this chapter assume no synchronized clocks, only bounded message delay for liveness. Safety never depends on timing: an unlucky clock can delay a decision but cannot cause two replicas to decide differently. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.119 Failure Detection (continued, part 12)\nA failure detector labels a replica as suspected after a timeout. Detectors are unreliable: a slow node and a dead node look identical from the outside, which is why safety must not depend on the detector's verdict. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.120 Batching and Pipelining (continued, part 12)\nThroughput improves when the leader batches many client requests into one log entry and pipelines several unacknowledged batches. Latency for a single request gets slightly worse; total throughput rises until the network or disk saturates. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.121 Storage Durability (continued, part 12)\nAn entry counts as stored only after fsync returns. Systems that skip fsync trade durability for latency and can lose committed entries in a correlated power failure, which breaks the majority argument the protocol relies on. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.122 Client Sessions (continued, part 12)\nA client retries a request after a timeout, so the state machine must deduplicate. Each client carries a session id and a monotonic sequence number, and the state machine remembers the last applied sequence number per session. In practice the details differ per system, but the invariant above is what every implementation has to preserve.\n\n12.123 Deadlock in the Lock Manager\nA distributed lock manager can deadlock when two transactions hold one lock each and request the other. The four Coffman conditions — mutual exclusion, hold and wait, no preemption and circular wait — must all hold. A wait-for graph detects the cycle; wound-wait and wait-die prevent it by ordering transactions by timestamp. None of this appears anywhere earlier in the chapter.\n"
        },
        "expectSections": [
          "Consensus",
          "Raft",
          "Quorum",
          "Replication"
        ],
        "expectTerms": [
          "Raft",
          "Paxos",
          "RPC",
          "CAP"
        ],
        "absentFacts": [
          "교착상태",
          "Coffman",
          "wait-for"
        ],
        "expectFormula": "t_{elect}",
        "expectTruncated": true,
        "whyHard": "원문이 SUMMARY_MAX_CHARS(48,000자)를 넘어 잘려서 들어간다. 기대 절·용어·수식은 전부 앞 48,000자 안에 있고, 상한 뒤에는 교착상태(Coffman 조건·wait-for 그래프)만 둔다 — 요약에 그 주제가 나오면 잘림이 실제로 일어나지 않았거나 모델이 교과서 지식을 끌어온 것이다. 잘림이 정상인 유일한 케이스라 expectTruncated: true 다."
      },
      "out": {
        "markdown": "# 요약\n분산 시스템에서 합의(Consensus)는 일부 노드가 고장 나더라도 복제본들이 단일 값에 동의하도록 보장하는 핵심 기술이다. 이를 구현하는 대표적인 프로토콜인 Raft는 리더 중심의 구조를 통해 시스템의 일관성과 가용성을 관리한다. 데이터의 안전한 복제와 상태 유지를 위해 쿼럼, 로그 압축, 멤버십 변경 등 다양한 기법이 활용된다.\n\n## 1. 합의(Consensus)\n- 합의는 $N$개의 복제본이 고장 상황에서도 하나의 값에 동의하게 만드는 프로토콜이다.\n- 프로토콜은 합의, 유효성, 종료라는 세 가지 조건을 보장해야 한다.\n- 초기에는 팍소스(Paxos)가 실용적인 프로토콜로 사용되었으나, 현재는 구현이 용이한 래프트(Raft)가 주로 쓰인다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 여러 대의 컴퓨터가 의견이 갈리지 않고 하나의 결론을 내리는 약속이다.\n>\n> #### 핵심 보장\n> - **합의:** 모든 복제본이 동일한 값을 결정함.\n> - **유효성:** 결정된 값은 누군가 제안한 값임.\n\n## 2. 래프트(Raft)\n- 시간 흐름을 임기(Term) 단위로 나누어 관리하며, 각 임기는 리더 선출로 시작된다.\n- 리더가 없는 팔로워는 선거 제한 시간(Election timeout)이 지나면 후보자가 되어 투표를 요청한다.\n- 선거 제한 시간은 무작위로 설정하여 여러 후보가 동시에 선거를 시작하는 충돌을 방지한다.\n- 리더는 로그를 복제하고, 과반수(Majority)가 저장하면 해당 항목을 커밋한다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 리더를 뽑아 그 리더가 정한 순서대로 데이터를 기록하게 하는 방식이다.\n>\n> #### 리더의 역할\n> - **로그 관리:** 클라이언트의 요청을 순서대로 기록함.\n> - **데이터 복제:** 과반수 이상의 복제본에 데이터가 저장되면 완료 처리함.\n\n## 3. 쿼럼(Quorum)\n- $N$개의 복제본 시스템에서 쓰기 쿼럼 $W$와 읽기 쿼럼 $R$이 $W + R > N$을 만족하면 항상 최신 데이터를 얻을 수 있다.\n- $W=N$으로 설정하면 쓰기는 느려지지만 읽기는 빨라지며, $W=R=\\lceil (N+1)/2 \\rceil$은 두 작업의 균형을 맞춘다.\n- CAP 정리(CAP theorem)에 따라 네트워크 분할 시 일관성이나 가용성 중 하나를 선택해야 하며, 쿼럼 크기가 그 절충안을 결정한다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 최소 몇 명의 동의가 있어야 일을 처리할지 정하는 규칙이다.\n>\n> #### 쿼럼의 균형\n> - **쓰기 중심:** 쓰기 쿼럼을 크게 잡아 데이터 안전성을 높임.\n> - **읽기 중심:** 읽기 쿼럼을 작게 잡아 조회 속도를 높임.\n\n## 4. 복제(Replication)\n- 동기식 복제는 모든 팔로워가 저장한 뒤 응답하므로 안전하지만 속도가 느리다.\n- 비동기식 복제는 즉시 응답하여 빠르지만, 리더 고장 시 데이터 유실 가능성이 있다.\n- 체인 복제(Chain replication)는 복제본을 일렬로 배치하여 헤드에서 쓰기를 받고 테일에서 읽기를 처리한다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 데이터를 여러 곳에 복사해두는 방식인데, 얼마나 빨리 복사할지 혹은 얼마나 안전하게 복사할지를 선택하는 문제다.\n>\n> #### 복제 방식\n> - **동기식:** 모두가 확인해야 끝남.\n> - **비동기식:** 리더만 확인하고 바로 끝냄.\n\n## 5. 로그 압축(Log Compaction)\n- 복제된 로그는 무한히 커질 수 없으므로, 주기적으로 상태 머신의 스냅샷을 찍고 이전 로그를 삭제한다.\n- 각 복제본은 독립적으로 스냅샷을 생성한다.\n- 리더는 너무 뒤처진 팔로워에게만 스냅샷을 전송하여 동기화한다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 기록이 너무 길어지면 중간중간 요약본(스냅샷)을 만들고 이전 기록은 버리는 것이다.\n>\n> #### 스냅샷의 목적\n> - **저장 공간 확보:** 불필요한 로그 삭제.\n> - **빠른 복구:** 뒤처진 노드를 빠르게 최신 상태로 만듦.\n\n## 6. 멤버십 변경(Membership Change)\n- 노드를 추가하거나 제거할 때 모든 노드의 설정을 동시에 바꾸는 것은 불가능하다.\n- 공동 합의(Joint-consensus) 방식을 사용하여 구 설정과 신 설정의 과반수 동의를 모두 얻는 중간 단계를 거친다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 팀원을 교체할 때 혼란을 막기 위해 기존 팀과 새 팀의 동의를 모두 받는 과도기를 두는 것이다.\n>\n> #### 안전한 교체\n> - **중간 단계:** 구 설정과 신 설정이 공존하는 시기를 가짐.\n> - **독립 운영:** 중간 단계가 완료된 후 새 설정만으로 운영함.",
        "sectionHits": 4,
        "termHits": 3,
        "absentHits": [],
        "formulaKept": false,
        "korean": true,
        "truncated": true,
        "truncationMismatch": false,
        "charsPerSection": 405,
        "calloutSections": 6,
        "calloutCompliant": 6,
        "ttftMs": 10519,
        "totalMs": 17364
      },
      "latencyMs": 17364
    },
    {
      "fixture": {
        "id": "term-trap",
        "input": {
          "sourceTitle": "Chapter 9. Virtual Memory and I/O",
          "sourceText": "Chapter 9. Virtual Memory and I/O\n\n9.1 Address Translation\nThe MMU translates a virtual address into a physical address using the page table. Each entry holds a frame number and protection bits. A page table walk costs one extra memory reference, so the MMU caches recent translations in the TLB.\n\n9.2 TLB Performance\nLet $h$ be the TLB hit ratio, $t_{mem}$ the memory access time, and $t_{walk}$ the page table walk time. The effective access time is\n$$EAT = h \\cdot t_{mem} + (1 - h) \\cdot (t_{walk} + t_{mem})$$\nWith $h = 0.98$ and $t_{mem} = 100$ ns the walk cost is largely amortized away.\n\n9.3 Page Replacement\nWhen no free frame remains, the OS evicts a page. FIFO evicts the oldest page and can suffer Belady's anomaly. LRU evicts the least recently used page and approximates OPT. Clock, also called second chance, approximates LRU with a single reference bit per frame.\n\n9.4 DMA and Interrupts\nA DMA controller moves blocks between a device and RAM without the CPU copying each word. The device raises an IRQ when the transfer completes and the CPU then runs the ISR. Programmed I/O, in contrast, busy-waits on a status register and burns CPU cycles."
        },
        "expectSections": [
          "Address Translation",
          "TLB",
          "Page Replacement",
          "DMA"
        ],
        "expectTerms": [
          "MMU",
          "TLB",
          "LRU",
          "FIFO",
          "DMA",
          "IRQ",
          "ISR"
        ],
        "absentFacts": [
          "스레싱",
          "작업 집합",
          "세그먼테이션"
        ],
        "expectFormula": "t_{mem}",
        "whyHard": "약어마다 한국어 정착 번역어가 있다(MMU 메모리 관리 장치, TLB 변환 색인 버퍼, LRU 최근 최소 사용, FIFO 선입선출, DMA 직접 메모리 접근, IRQ 인터럽트 요청, ISR 인터럽트 서비스 루틴). 번역어만 쓰면 사용자가 슬라이드로 돌아갔을 때 대응 항목을 못 찾는다 — 병기해야 통과한다."
      },
      "out": {
        "markdown": "# 요약\n가상 메모리 시스템은 메모리 관리 장치를 통해 가상 주소를 물리 주소로 변환하며, 변환 효율을 높이기 위해 변환 색인 버퍼를 사용한다. 페이지 교체 알고리즘은 메모리가 부족할 때 어떤 페이지를 내보낼지 결정하며, 입출력 작업은 중앙 처리 장치의 부담을 줄이기 위해 직접 메모리 접근 방식을 활용한다.\n\n## 1. 주소 변환(Address Translation)\n- 메모리 관리 장치(MMU)는 페이지 테이블을 사용하여 가상 주소를 물리 주소로 변환한다.\n- 페이지 테이블의 각 항목에는 프레임 번호와 보호 비트가 포함된다.\n- 페이지 테이블을 직접 참조하는 비용을 줄이기 위해, 최근 변환 기록을 변환 색인 버퍼(TLB)에 저장한다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 주소 변환은 가상 주소라는 별명을 실제 메모리 위치라는 본명으로 바꾸는 과정이다.\n>\n> #### 변환 과정\n> - **MMU:** 가상 주소를 물리 주소로 바꾸는 번역기 역할을 한다.\n> - **TLB:** 자주 쓰는 번역 결과를 미리 적어두는 메모장이다.\n\n## 2. 변환 색인 버퍼 성능(TLB Performance)\n- 유효 접근 시간(EAT)은 TLB 적중률($h$), 메모리 접근 시간($t_{mem}$), 페이지 테이블 탐색 시간($t_{walk}$)에 의해 결정된다.\n- 유효 접근 시간 공식은 다음과 같다.\n$$EAT = h \\cdot t_{mem} + (1 - h) \\cdot (t_{walk} + t_{mem})$$\n- 적중률이 높을수록 페이지 테이블을 탐색하는 비용이 크게 줄어든다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** TLB를 잘 활용하면 메모리 주소를 찾는 시간을 획기적으로 줄일 수 있다.\n>\n> #### 성능 지표\n> - **적중률:** TLB에서 주소를 바로 찾을 확률이다.\n> - **탐색 비용:** TLB에 없을 때 페이지 테이블을 뒤지는 시간이다.\n\n## 3. 페이지 교체(Page Replacement)\n- 운영 체제는 빈 프레임이 없을 때 기존 페이지를 내보내야 한다.\n- 선입선출(FIFO)은 가장 오래된 페이지를 제거하며, 벨레이디의 모순(Belady's anomaly)이 발생할 수 있다.\n- 최근 최소 사용(LRU)은 가장 오랫동안 사용되지 않은 페이지를 제거하며 최적 알고리즘(OPT)과 유사한 성능을 낸다.\n- 시계(Clock) 알고리즘은 참조 비트를 사용하여 최근 최소 사용 알고리즘을 간소화한 방식이다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** 메모리가 꽉 찼을 때 어떤 데이터를 버리고 새 데이터를 넣을지 정하는 규칙이다.\n>\n> #### 교체 방식\n> - **FIFO:** 먼저 들어온 것을 먼저 버린다.\n> - **LRU:** 가장 안 쓴 것을 버린다.\n> - **Clock:** 참조 비트를 확인해 기회를 한 번 더 준다.\n\n## 4. 직접 메모리 접근 및 인터럽트(DMA and Interrupts)\n- 직접 메모리 접근(DMA) 제어기는 중앙 처리 장치(CPU)의 개입 없이 장치와 메모리 사이의 데이터를 이동시킨다.\n- 데이터 전송이 완료되면 장치는 인터럽트 요청(IRQ)을 보내고, 중앙 처리 장치는 인터럽트 서비스 루틴(ISR)을 실행한다.\n- 프로그램 입출력(Programmed I/O)은 중앙 처리 장치가 상태 레지스터를 계속 확인하며 대기하므로 자원 낭비가 발생한다.\n\n> [!easy] 쉬운 설명\n> **한마디로:** DMA는 중앙 처리 장치 대신 데이터를 옮겨주는 심부름꾼이다.\n>\n> #### 입출력 방식\n> - **DMA:** CPU가 다른 일을 하는 동안 데이터를 직접 옮긴다.\n> - **프로그램 입출력:** CPU가 데이터가 올 때까지 계속 확인만 하느라 다른 일을 못 한다.",
        "sectionHits": 4,
        "termHits": 7,
        "absentHits": [],
        "formulaKept": true,
        "korean": true,
        "truncated": false,
        "truncationMismatch": false,
        "charsPerSection": 314,
        "calloutSections": 4,
        "calloutCompliant": 4,
        "ttftMs": 4489,
        "totalMs": 11125
      },
      "latencyMs": 11125
    }
  ],
  "runAt": "2026-08-05T12:37:39.966Z"
}